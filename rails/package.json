{
  "name": "app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --format=esm --outdir=app/assets/builds --public-path=/rails/assets"
  },
  "dependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo-rails": "^8.0.16",
    "@knight-school/chess-core": "file:../packages/chess-core",
    "cm-chessboard": "^8.12.19"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
