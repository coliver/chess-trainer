name: angular

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  angular:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: angular

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: angular/package-lock.json

      # Angular depends on @knight-school/chess-core via file:../packages/chess-core,
      # so build the package first (its own isolated install + tsup build).
      - name: Build chess-core package
        working-directory: packages/chess-core
        run: npm ci && npm run build

      # Clean, reproducible install straight from the lockfile.
      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npx ng lint

      - name: Build
        run: npx ng build

      - name: Unit tests (headless Chrome)
        run: npx ng test --watch=false --browsers=ChromeHeadlessNoSandbox
